# Завдання 8. Генератор комбінованих даних зі списку і словника
# Дано: 1) список імен: names = ["Іван", "Ольга", "Богдан"]; 2) словник оцінок: scores = {"Іван": 90, "Ольга": 85, "Богдан": 78}.
# Написати генераторну функцію name_score_pairs(names: list[str], scores: dict[str, int]), яка:
# 1) проходить по списку імен; 2) для кожного імені повертає кортеж (name, score);
# 3) якщо ім’я відсутнє в словнику оцінок – пропускає його або повертає (name, None) (оберіть один варіант і задокументуйте його в docstring).

def name_score_pairs(names, scores):
    """
    Повертає (name, score). Якщо імені нема в словнику — повертає (name, None).
    """
    for name in names:
        yield (name, scores.get(name, None))


# --- Ввід ---
names = input("Введіть імена через пробіл: ").split()
import ast
scores = ast.literal_eval(input("Введіть словник оцінок: "))

for pair in name_score_pairs(names, scores):
    print(pair)
