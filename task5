# Завдання 5. Генератор для "плаского" обходу вкладених списків
# Є список, елементи якого можуть бути: або числами, або вкладеними списками чисел, наприклад data = [1, [2, 3], [4, [5, 6]], 7]
# Написати генераторну функцію flatten(data), яка:
# "розплющує" такі структури і послідовно генерує всі числа: для прикладу вище → 1, 2, 3, 4, 5, 6, 7.

def flatten(data):
    """Рекурсивно розплющує вкладені списки."""
    for item in data:
        if isinstance(item, list):
            yield from flatten(item)
        else:
            yield item


# --- Ввід ---
import ast
data = ast.literal_eval(input("Введіть список (наприклад: [1,[2,3]]): "))

print("Плоский список:")
for x in flatten(data):
    print(x, end=" ")
