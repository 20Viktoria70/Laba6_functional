# Завдання 3. Генератор підрядків рядка
#Написати генераторну функцію substrings(s: str), яка послідовно генерує всі підрядки заданого рядка s фіксованої довжини k.
# 1) Функція має вигляд: substrings(s: str, k: int). 
# 2) Якщо k більше за довжину рядка – генератор не має повертати жодного значення.
# 3) Продемонструвати роботу з рядком "abcdef" і k = 3 → "abc", "bcd", "cde", "def".

def substrings(s: str, k: int):
    """Генерує підрядки довжини k."""
    if k > len(s):
        return
    for i in range(len(s) - k + 1):
        yield s[i:i+k]


# --- Ввід ---
s = input("Введіть рядок: ")
k = int(input("Введіть довжину підрядка k: "))

print("Підрядки:")
for sub in substrings(s, k):
    print(sub)
