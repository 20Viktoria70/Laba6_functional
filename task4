# Завдання 4. Перетворення списку кортежів у словник
# Дано список кортежів: pairs = [("apple", 3), ("banana", 5), ("apple", 2), ("orange", 4)]
# Потрібно: 1) написати функцію aggregate_pairs(pairs: list[tuple[str, int]]) -> dict[str, int], яка:
# 1.1 формує словник product -> total_quantity; 1.2 однакові ключі агрегуються (у прикладі "apple" → 3 + 2 = 5).
# 2) використати перебір списку та роботу зі словником (без Counter).

def aggregate_pairs(pairs):
    result = {}
    for key, value in pairs:
        result[key] = result.get(key, 0) + value
    return result

pairs = []
n = int(input("Скільки кортежів ввести? "))

for _ in range(n):
    key = input("Введіть ключ: ")
    value = int(input("Введіть число: "))
    pairs.append((key, value))

print("Результат:", aggregate_pairs(pairs))
